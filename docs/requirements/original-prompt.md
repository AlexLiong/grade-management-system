# Task
请你根据这个要求pdf文档，完整且详尽的完成这个软件实践设计任务

# Requirements
- 完整且详尽得实现这个系统中关于成绩管理、后台远程方法调用以及系统安全性等各个方面的具体要求，确保按照各个要求漂亮地完成
- 特别注重系统的分布式对象设计以及安全性方面。分离业务层和数据访问层到不同模块，并实现一个轻量的网关中台以满足不同服务之前的注册发现与远程调用，使得服务解耦后可弹性扩缩。单独设计一个的数据访问层模块。并结合业务规模按需引入SpringCloud组件
- 部分模块可根据实际业务需求酌情扩展功能，但请注意一定不能背离系统要求本身
- 使用java springboot + vue的前后端设计，确保系统能在idea和nodejs中中直接运行
- 代码生成工程中要有中间文档，注意保留原始需求完整且清晰，各个功能模块需具备健壮性
- 在编写程序完毕后，必须对程序所有功能做完整并且详尽的测试，如果遇到了bug请及时修复，确保输出的程序可以完整无误地实现所有功能
  
# output
- 需要你制作完整的程序（包括前后端等各个模块），给出完整的程序源码，确保可以直接在windows，macos等系统环境下运行
- 需要如下的文档，均以markdown格式输出：(先不需要写答辩先关的文档)
1. readme针对项目的基本说明
2. 整体的架构说明文档，包括程序的项目概述、整体架构、各个功能类的结构与职责，分布式以及安全的架构设计等
3. 关于各个类的详细的功能说明
4. 各种程序设计中的模型图，包括用例图，uml图等，确保图与代码中各个类的严格一一对应