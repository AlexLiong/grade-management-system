# Production normally serves the frontend and API from one HTTPS origin.
VITE_API_BASE_URL=/api
# Only used when running Vite against the backend production TLS profile.
VITE_API_TARGET=https://localhost:8443
